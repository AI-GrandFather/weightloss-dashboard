This project's harness targets Antigravity CLI. AGENTS.md at project root is the source of
truth for stack, rules, and scope — read that instead. This file exists only so Claude Code
doesn't start from zero if you ever open this repo in it.
